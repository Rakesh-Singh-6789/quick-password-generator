# Password Generator

A simple Chrome extension for generating random passwords. Everything runs locally in your browser with no backend or data collection.

Supports uppercase letters, lowercase letters, numbers, and symbols. Password length ranges from 8 to 32 characters.

To install: Load the extension folder as an unpacked extension in Chrome (chrome://extensions, enable Developer mode, then click Load unpacked(complete folder to chrome/brave or any other chromium based browser)).

